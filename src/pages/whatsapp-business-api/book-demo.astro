---
import WhatsappLayout from "../../layouts/whatsapp-business-api/WhatsappLayout.astro";
import "../../../public/whatsapp-business-api/css/whatsAppBusiness.css";
import LogoStrips from "../../components/LogoStrips.astro";
import PhoneApp from "../../components/PhoneApp.astro";

const metaTitle = "Book a Demo | WhatsApp Business API by Emovur";
const metaDescription =
  "Experience the power of WhatsApp Business API with Emovur. Book a free demo to see how you can automate, engage, and grow using WhatsApp for your business.";
const canonicalUrl = "https://emovur.com/whatsapp-business-api/book-demo";
const keywords =
  "book demo, whatsapp api demo, emovur product tour, chatbot demo, marketing automation demo.";
---

<WhatsappLayout
  title={metaTitle}
  description={metaDescription}
  canonical={canonicalUrl}
  keywords={keywords}
>
  <div
    style="width:100%;height:100%;position:relative;top:150px;overflow:scroll; padding: 50px 0px;margin-bottom: 150px;"
    id="my-cal-inline"
  >
  </div>
  <LogoStrips />
</WhatsappLayout>

<script type="text/javascript">
  (function (C, A, L) {
    let p = function (a, ar) {
      a.q.push(ar);
    };
    let d = C.document;
    C.Cal =
      C.Cal ||
      function () {
        let cal = C.Cal;
        let ar = arguments;
        if (!cal.loaded) {
          cal.ns = {};
          cal.q = cal.q || [];
          d.head.appendChild(d.createElement("script")).src = A;
          cal.loaded = true;
        }
        if (ar[0] === L) {
          const api = function () {
            p(api, arguments);
          };
          const namespace = ar[1];
          api.q = api.q || [];
          if (typeof namespace === "string") {
            cal.ns[namespace] = cal.ns[namespace] || api;
            p(cal.ns[namespace], ar);
            p(cal, ["initNamespace", namespace]);
          } else p(cal, ar);
          return;
        }
        p(cal, ar);
      };
  })(window, "https://bookings.emovur.com/embed/embed.js", "init");
  Cal("init", "whatsapp-business-api-one-on-one-demo", {
    origin: "https://bookings.emovur.com",
  });

  Cal.ns["whatsapp-business-api-one-on-one-demo"]("inline", {
    elementOrSelector: "#my-cal-inline",
    config: {
      layout: "month_view",
      theme: "light",
    },
    calLink: "team/bdm/whatsapp-business-api-one-on-one-demo",
  });

  Cal.ns["whatsapp-business-api-one-on-one-demo"]("ui", {
    theme: "light",
    cssVarsPerTheme: {
      light: {
        "cal-brand": "#0c9ffa",
      },
      dark: {
        "cal-brand": "#0c9ffa",
      },
    },
    hideEventTypeDetails: false,
    layout: "month_view",
  });
</script>
<!-- Cal inline embed code ends -->

<script>
  function hideLoader() {
    var loader = document.getElementById("loader");
    setTimeout(function () {
      loader.style.display = "none";
    }, 2000);
  }
  window.onload = hideLoader;
</script>

<!-- Script JS -->
<script src="./js/script.js" is:inline></script>
